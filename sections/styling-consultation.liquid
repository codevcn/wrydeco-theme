{%- liquid
  assign lead_designer_entry = metaobjects.lead_designer.values | first
  assign lead_designer_avatar_url = lead_designer_entry.avatar_img_url.value
  assign lead_designer_name = lead_designer_entry.name.value
  assign lead_designer_slogan = lead_designer_entry.slogan.value
-%}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-block: var(--section-padding-y-lg, 100px);
  }

  @media screen and (min-width: 768px) {
    .section-{{ section.id }}-padding {
      padding-left: var(--container-padding-x, 48px);
      padding-right: var(--container-padding-x, 48px);
    }
  }

  .styling-consultation {
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
    color: var(--text-primary, #1a1815);
    background-color: #f1eae2; /* Fallback color */
    background-size: cover;
    background-position: left bottom;
    background-repeat: no-repeat;
  }

  {% if section.settings.bg_image != blank %}
  .styling-consultation {
    background-image: url('{{ section.settings.bg_image | image_url: width: 2000 }}');
  }
  {% else %}
  /* Fallback preset image */
  .styling-consultation {
    background-image: url('{{ 'private-styling-consultation.png' | file_url }}');
  }
  {% endif %}

  /* Optional overlay for text readability */
  .styling-consultation::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, rgba(247, 242, 236, 0.9), rgba(247, 242, 236, 0.68) 52%, rgba(247, 242, 236, 0.18));
    pointer-events: none;
  }

  .styling-consultation__inner {
    width: min(calc(100% - 48px), var(--page-max-width, 1440px));
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    gap: var(--sp-10, 48px);
  }

  @media screen and (min-width: 900px) {
    .styling-consultation__inner {
      flex-direction: row;
      align-items: stretch;
      justify-content: space-between;
      gap: 3vw;
    }
  }

  @media screen and (min-width: 100rem) {
    .styling-consultation__inner {
      justify-content: center;
    }
  }

  /* Left Column */
  .styling-consultation__left {
    flex: 1;
    max-width: 620px;
  }

  .styling__eyebrow {
    font-family: var(--font-body);
    font-size: var(--eyebrow-size, 12px);
    letter-spacing: var(--eyebrow-tracking, 0.3em);
    font-weight: 600;
    margin-bottom: 14px;
    text-transform: uppercase;
    color: var(--color-brown, #4a2c18);
    display: block;
  }

  .styling__heading {
    font-family: var(--font-heading);
    font-size: var(--section-heading-size, 48px);
    font-weight: var(--font-weight-light, 300);
    line-height: var(--section-heading-line, 1.02);
    margin: 0 0 18px 0;
    letter-spacing: var(--section-heading-tracking, -0.02em);
    color: var(--color-text, #2b1a11);
  }

  .styling__desc {
    font-family: var(--font-body);
    font-size: var(--section-subtitle-size, 16px);
    line-height: 1.6;
    color: var(--color-muted, #221f1d);
    margin: 0 0 22px 0;
    max-width: 58ch;
  }

  .styling__custom-list {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin: 0 0 22px;
    padding: 0;
    list-style: none;
  }

  .styling__custom-list li {
    margin: 0;
    padding: 8px 11px;
    border: 1px solid rgba(74, 44, 24, 0.16);
    border-radius: var(--radius-full);
    background-color: rgba(247, 242, 236, 0.72);
    color: var(--color-earth-brown);
    font-family: var(--font-body);
    font-size: 12px;
    font-weight: var(--font-weight-semibold);
    letter-spacing: 0.02em;
  }

  .styling__service-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
    margin-bottom: 18px;
  }

  .styling__service-item {
    display: flex;
    align-items: center;
    background: rgba(255, 255, 255, 0.4);
    border: 1px solid rgba(74, 44, 24, 0.12);
    border-radius: var(--radius-lg, 8px);
    padding: 14px 16px;
    transition: background 0.3s var(--ease-standard), transform 0.3s var(--ease-standard);
    cursor: pointer;
    text-decoration: none;
    color: inherit;
  }

  .styling__service-item:hover {
    background: rgba(255, 255, 255, 0.7);
    transform: translateY(-2px);
  }

  .styling__service-icon {
    flex: 0 0 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    border: 1px solid rgba(107, 93, 82, 0.2);
    margin-right: 16px;
  }

  .styling__service-icon svg {
    width: 24px;
    height: 24px;
    opacity: 0.8;
  }

  .styling__service-text {
    flex: 1;
  }

  .styling__service-title {
    font-family: var(--font-body);
    font-size: 14px;
    font-weight: 600;
    margin: 0 0 4px 0;
  }

  .styling__service-desc {
    font-family: var(--font-body);
    font-size: 12px;
    color: var(--text-secondary, #6b5d52);
    margin: 0;
    line-height: 1.4;
  }

  .styling__service-arrow {
    margin-left: 16px;
    opacity: 0.5;
  }

  .styling__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: var(--btn-height, 52px);
    background-color: var(--color-brown, #4a2c18);
    color: #fcf9f6;
    font-family: var(--font-body);
    font-size: var(--btn-font-size, 14px);
    font-weight: var(--font-weight-medium, 500);
    padding: 0 var(--btn-padding-x, 32px);
    border-radius: var(--btn-radius, 4px);
    text-decoration: none;
    transition: transform 0.3s ease, background-color 0.3s ease;
    border: none;
    cursor: pointer;
    width: 100%;
  }

  @media screen and (min-width: 600px) {
    .styling__btn {
      width: auto;
    }
  }

  .styling__btn:hover {
    color: #f1eae2;
    background-color: var(--color-brown-dark, #2f1a0d);
    transform: translateY(-2px);
  }

  .styling__btn svg {
    margin-left: 12px;
    width: 16px;
    height: 16px;
  }

  /* Right Column (Advisor Card) */
  .styling-consultation__right {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  @media screen and (min-width: 100rem) {
    .styling-consultation__right {
      flex: none;
    }
  }

  .advisor-card {
    display: flex;
    flex-direction: column;
    width: min(100%, 580px);
    background-color: #6c533f;
    color: #f1eae2;
    border-radius: var(--radius-xl, 12px);
    overflow: hidden;
    box-shadow: 0 28px 60px rgba(74, 44, 24, 0.22);
  }

  .advisor-card__top {
    padding: 32px;
    display: flex;
    gap: var(--sp-6, 24px);
    flex-direction: column;
  }

  @media screen and (min-width: 500px) {
    .advisor-card__top {
      flex-direction: row;
    }
  }

  .advisor-card__img-wrapper {
    flex: 0 0 230px;
    height: fit-content;
    border-radius: 8px;
    overflow: hidden;
  }

  .advisor-card__img {
    width: 100%;
    aspect-ratio: 3 / 4;
    object-fit: cover;
    border-radius: var(--radius-sm, 6px);
    display: block;
  }

  .advisor-card__info {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .advisor-card__eyebrow {
    font-size: 10px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    opacity: 0.9;
    margin-bottom: 12px;
  }

  .advisor-card__name {
    font-family: var(--font-display);
    font-size: 28px;
    margin: 0 0 4px 0;
    font-weight: 400;
    color: inherit;
  }

  .advisor-card__title {
    font-size: 12px;
    opacity: 0.8;
    word-spacing: 0.1em;
    margin: 0 0 20px 0;
  }

  .advisor-card__quote-mark {
    font-family: var(--font-display);
    font-size: 60px;
    font-weight: 700;
    line-height: 0.5;
    opacity: 0.8;
    margin-bottom: 8px;
    transform: translateY(15px);
  }

  .advisor-card__quote {
    font-size: 14px;
    font-style: italic;
    font-family: var(--font-display);
    line-height: 1.5;
    opacity: 0.9;
    margin: 0;
  }

  .advisor-card__divider {
    height: 2.5px;
    background-color: rgba(242, 232, 220, 0.26);
    margin: 0 32px;
  }
  .advisor-card__divider.under-title {
    margin: 0 0 10px 0;
  }

  .advisor-card__stats {
    display: flex;
    padding: 24px 32px;
  }

  .advisor-card__stat-item {
    flex: 1;
    text-align: center;
    position: relative;
  }

  .advisor-card__stat-item:not(:last-child)::after {
    content: '';
    position: absolute;
    right: 0;
    top: 10%;
    height: 80%;
    width: 1px;
    background-color: rgba(241, 234, 226, 0.15);
  }

  .advisor-card__stat-num {
    font-family: var(--font-roboto);
    font-size: 24px;
    margin: 0 0 4px 0;
    color: #f1eae2;
    opacity: 0.8;
  }

  .advisor-card__stat-label {
    font-size: 10px;
    opacity: 0.7;
    margin: 0;
  }

  .advisor-card__bottom {
    background-color: #3b2f26; /* Darker brown */
    padding: 24px 32px;
    display: flex;
    align-items: center;
    gap: 20px;
  }

  .advisor-card__bottom-icon {
    flex: 0 0 40px;
    height: 40px;
    opacity: 0.6;
  }

  .advisor-card__bottom-icon svg {
    width: 100%;
    height: 100%;
  }

  .advisor-card__bottom-text h4 {
    font-size: 12px;
    letter-spacing: 0.15em;
    margin: 0 0 4px 0;
    font-weight: 600;
    color: #f1eae2;
    opacity: 0.8;
  }

  .advisor-card__bottom-text p {
    font-size: 14px;
    opacity: 0.8;
    margin: 0;
    line-height: 1.4;
    color: #f1eae2;
  }

{%- endstyle -%}

<div class="styling-consultation section-{{ section.id }}-padding">
  <div class="styling-consultation__inner page-width">
    <!-- Left Column -->
    <div class="styling-consultation__left">
      <span class="styling__eyebrow">{{ section.settings.eyebrow }}</span>
      <h2 class="styling__heading">{{ section.settings.heading | newline_to_br }}</h2>
      <p class="styling__desc">{{ section.settings.description }}</p>

      <ul class="styling__custom-list" aria-label="Customization options">
        <li>Custom size</li>
        <li>Wood species</li>
        <li>Finish color</li>
        <li>Shape &amp; form</li>
        <li>Edge detail</li>
        <li>Shelving layout</li>
        <li>Sketch approval</li>
      </ul>

      <div class="styling__service-list">
        {%- for block in section.blocks -%}
          <a href="{{ section.settings.button_link | default: '#' }}" class="styling__service-item" {{ block.shopify_attributes }}>
            <div class="styling__service-icon">
              {%- if block.settings.icon == 'blueprint' -%}
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <path d="M4 4h16v16H4z M4 9h16 M9 4v16"/>
                  <circle cx="14" cy="14" r="1.5" fill="currentColor"/>
                </svg>
              {%- elsif block.settings.icon == 'video' -%}
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round">
                  <rect x="2" y="6" width="14" height="12" rx="2"/>
                  <path d="M22 8l-6 4 6 4V8z"/>
                </svg>
              {%- else -%}
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <circle cx="12" cy="12" r="9" stroke-dasharray="2 2"/>
                  <circle cx="12" cy="12" r="5"/>
                  <circle cx="12" cy="12" r="1.5" fill="currentColor"/>
                </svg>
              {%- endif -%}
            </div>
            <div class="styling__service-text">
              <h3 class="styling__service-title">{{ block.settings.title }}</h3>
              <p class="styling__service-desc">{{ block.settings.description }}</p>
            </div>
            <div class="styling__service-arrow">
              <svg
                width="20"
                height="20"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="1.5"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <path d="M5 12h14M12 5l7 7-7 7"/>
              </svg>
            </div>
          </a>
        {%- endfor -%}
      </div>

      <a href="{{ section.settings.button_link | default: '#' }}" class="styling__btn">
        {{ section.settings.button_label }}
        <svg
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
    </div>

    <!-- Right Column -->
    <div class="styling-consultation__right">
      <div class="advisor-card">
        <div class="advisor-card__top">
          <div class="advisor-card__img-wrapper">
            {%- if lead_designer_avatar_url != blank -%}
              <img
                src="{{ lead_designer_avatar_url | escape }}"
                alt="{{ lead_designer_name | default: 'WRYDECO lead designer' | escape }}"
                class="advisor-card__img"
                loading="lazy"
                width="400"
                height="533"
              >
            {%- else -%}
              <img
                src="{{ 'design-advisor.png' | file_url }}"
                alt="Mr. Nguyen"
                class="advisor-card__img"
                loading="lazy"
                width="400"
                height="533"
              >
            {%- endif -%}
          </div>
          <div class="advisor-card__info">
            <span class="advisor-card__eyebrow">{{ section.settings.advisor_eyebrow }}</span>
            <h3 class="advisor-card__name">{{ lead_designer_name | escape }}</h3>
            <p class="advisor-card__title">{{ section.settings.advisor_title }}</p>
            <span class="advisor-card__divider under-title"></span>
            <div class="advisor-card__quote-mark">“</div>
            <p class="advisor-card__quote">{{ lead_designer_slogan | escape }}</p>
          </div>
        </div>

        <div class="advisor-card__divider"></div>

        <div class="advisor-card__stats">
          <div class="advisor-card__stat-item">
            <h4 class="advisor-card__stat-num">{{ section.settings.stat_1_num }}</h4>
            <p class="advisor-card__stat-label">{{ section.settings.stat_1_label }}</p>
          </div>
          <div class="advisor-card__stat-item">
            <h4 class="advisor-card__stat-num">{{ section.settings.stat_2_num }}</h4>
            <p class="advisor-card__stat-label">{{ section.settings.stat_2_label }}</p>
          </div>
          <div class="advisor-card__stat-item">
            <h4 class="advisor-card__stat-num">{{ section.settings.stat_3_num }}</h4>
            <p class="advisor-card__stat-label">{{ section.settings.stat_3_label }}</p>
          </div>
        </div>

        <div class="advisor-card__bottom">
          <div class="advisor-card__bottom-icon">
            <svg viewBox="0 0 40 40" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M20 5C12 5 5 12 5 20s7 15 15 15 15-7 15-15" stroke-dasharray="2 4"/>
              <path d="M20 10c-6 0-10 4-10 10s4 10 10 10 10-4 10-10" stroke-dasharray="4 4"/>
              <circle cx="20" cy="20" r="5"/>
              <circle cx="20" cy="20" r="2" fill="currentColor"/>
            </svg>
          </div>
          <div class="advisor-card__bottom-text">
            <h4>{{ section.settings.footer_heading }}</h4>
            <p>{{ section.settings.footer_text }}</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Styling Consultation",
  "tag": "section",
  "class": "section section-styling-consultation",
  "settings": [
    {
      "type": "image_picker",
      "id": "bg_image",
      "label": "Background Image"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "GUIDANCE FOR YOUR SPACE"
    },
    {
      "type": "textarea",
      "id": "heading",
      "label": "Heading",
      "default": "Private Styling\nConsultation"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Choosing a sculptural wooden piece is a personal journey. Our design experts help you find the perfect piece that complements your space, style, and story."
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button Label",
      "default": "Book a Private Consultation"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "header",
      "content": "Advisor Card"
    },
    {
      "type": "text",
      "id": "advisor_eyebrow",
      "label": "Advisor Eyebrow",
      "default": "YOUR DESIGN ADVISOR"
    },
    {
      "type": "text",
      "id": "advisor_title",
      "label": "Advisor Title",
      "default": "Lead Design Consultant"
    },
    {
      "type": "text",
      "id": "stat_1_num",
      "label": "Stat 1 Number",
      "default": "12+"
    },
    {
      "type": "text",
      "id": "stat_1_label",
      "label": "Stat 1 Label",
      "default": "Years of Experience"
    },
    {
      "type": "text",
      "id": "stat_2_num",
      "label": "Stat 2 Number",
      "default": "500+"
    },
    {
      "type": "text",
      "id": "stat_2_label",
      "label": "Stat 2 Label",
      "default": "Homes Styled"
    },
    {
      "type": "text",
      "id": "stat_3_num",
      "label": "Stat 3 Number",
      "default": "98%"
    },
    {
      "type": "text",
      "id": "stat_3_label",
      "label": "Stat 3 Label",
      "default": "Client Satisfaction"
    },
    {
      "type": "text",
      "id": "footer_heading",
      "label": "Card Footer Heading",
      "default": "BESPOKE. PERSONAL. EXCEPTIONAL."
    },
    {
      "type": "text",
      "id": "footer_text",
      "label": "Card Footer Text",
      "default": "Discreet service. Thoughtful guidance. Timeless results."
    }
  ],
  "blocks": [
    {
      "type": "service",
      "name": "Service",
      "limit": 3,
      "settings": [
        {
          "type": "select",
          "id": "icon",
          "label": "Icon",
          "options": [
            { "value": "blueprint", "label": "Blueprint/Room" },
            { "value": "video", "label": "Video Camera" },
            { "value": "wood", "label": "Wood Rings" }
          ],
          "default": "blueprint"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Service Title"
        },
        {
          "type": "text",
          "id": "description",
          "label": "Description",
          "default": "Service description goes here."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Private Styling Consultation",
      "blocks": [
        {
          "type": "service",
          "settings": {
            "icon": "blueprint",
            "title": "Share Your Space",
            "description": "Send us photos and measurements of your space. We'll provide tailored piece recommendations."
          }
        },
        {
          "type": "service",
          "settings": {
            "icon": "video",
            "title": "Schedule a Video Consultation",
            "description": "Discuss your vision with our design expert in a personalized 1:1 session."
          }
        },
        {
          "type": "service",
          "settings": {
            "icon": "wood",
            "title": "Trade & Design Support",
            "description": "For interior designers, architects, and trade professionals. Get dedicated support."
          }
        }
      ]
    }
  ]
}
{% endschema %}
